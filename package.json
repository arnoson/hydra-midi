{
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "cleanup": "shx rm -rf ./dist",
    "build": "npm run cleanup && tsup",
    "dev": "tsup --watch"
  },
  "devDependencies": {
    "@types/webmidi": "^2.0.6",
    "rollup": "^3.7.3",
    "shx": "^0.3.4",
    "terser": "^5.16.3",
    "tsup": "^6.6.0"
  }
}
