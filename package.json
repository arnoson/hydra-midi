{
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "cleanup": "shx rm -rf ./dist",
    "build": "npm run cleanup && tsup",
    "dev": "tsup --watch",
    "test:types": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/webmidi": "^2.0.7",
    "shx": "^0.3.4",
    "terser": "^5.16.5",
    "tsup": "^6.6.3",
    "typescript": "^4.9.5"
  }
}
